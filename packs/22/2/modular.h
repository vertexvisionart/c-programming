#ifndef MODULAR_H
#define MODULAR_H

// Объявление глобальной переменной для хранения модуля
extern int MOD;

// Объявления функций модульной арифметики
int pnorm(int a);
int padd(int a, int b);
int psub(int a, int b);
int pmul(int a, int b);
int pdiv(int a, int b);

#endif // MODULAR_H